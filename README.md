# CS178-goop-squad-CIFAR10

Dataset: [CIFAR-10](https://huggingface.co/datasets/uoft-cs/cifar10)

```bash
source .venv/bin/activate
uv pip install -r requirements.txt
```

Initialize the dataset using:

```bash
uv run init_data.py
```
