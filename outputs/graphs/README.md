Graphs output folder

Purpose:

- Store all generated figures and metric plots used in analyses and the final report.

Things like:
- Name plots with model and metric (e.g. `cnn_accuracy.png`, `kNN_confusion.png`).
- When possible, save both PNG and a vector format (SVG/PDF) for publication-quality images.
