Outputs directory

Purpose:

- Central location for produced artifacts: model weights, training logs, and graphs used in the report.

Structure:

- `model_weights/` — store saved model parameters and checkpoints.
- `graphs/` — store training curves, confusion matrices, and other visuals.

things like:

- Name files clearly (include model name and date/version) and include a short text file or README explaining contents when helpful.
