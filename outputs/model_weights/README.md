Model weights output folder

Purpose:

- Store saved model parameters (checkpoints, final weights) created by training scripts.

things like:

- Use descriptive filenames (e.g. `cnn_best.pth`, `logistic_final.pkl`).
- Track which training run produced each file (include a small `.txt` or `.json` with hyperparameters if helpful).
