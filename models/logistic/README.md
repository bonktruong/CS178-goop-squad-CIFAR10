Logistic regression folder

Purpose:

- Implement a logistic regression baseline for CIFAR-10 (flattened inputs or feature-reduced).

What to include:

- Training and evaluation scripts, preprocessing steps, and hyperparameter settings.
- Save final model weights to `outputs/model_weights` and performance plots to `outputs/graphs`.

Deliverable:

- Baseline results and scripts to reproduce them for inclusion in the final report.
