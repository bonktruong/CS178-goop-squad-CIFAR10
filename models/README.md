Models directory

Purpose:

- Contain model implementations and training / evaluation code for the assignment.

Structure and expectations:

- Subfolders: `CNN`, `kNN`, `logistic`, `feedforward`.
- Save trained weights to `outputs/model_weights` and graphs/metrics to `outputs/graphs`.

